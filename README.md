# badgeware-experiments

This repository holds experiments with programmable conference-badge hardware. Each experiment lives in its own top-level folder, with its own README.

## Experiments

| Folder | Purpose |
| --- | --- |
| `step-sync/` | Sync a step count and a short status message between two badges over Bluetooth. |

Add a new experiment as a new top-level folder, with its own README.
